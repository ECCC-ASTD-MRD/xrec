#!/bin/sh

if [[ $# -eq 0 ]] ; then
    echo "Usage:   . ./setup-xrec.dot [language]"
    echo "Example: . ./setup-xrec.dot english for English interface"
    echo "Example: . ./setup-xrec.dot francais pour l'interface en français"
else
    export ARMNLIB=$PWD/../work/armnlib
    export TMPDIR=${TMPDIR-$(mktemp -d /tmp/$USER-XXXX)}
    export CMCLNG=${1}
    echo "To use xrec: ./xrec -imflds [rpn_fst_file]"
fi
